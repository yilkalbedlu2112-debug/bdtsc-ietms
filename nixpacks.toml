[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqli", "composer"]

[phases.build]
cmds = ["composer install --no-dev --ignore-platform-reqs"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."